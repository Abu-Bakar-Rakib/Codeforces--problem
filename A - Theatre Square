#include <iostream>
using namespace std;
int main() 
{
    long long n,m,a;
    cin>>n>>m>>a;
    long long horizontal = (n + a - 1) / a;
    long long vertical = (m + a - 1) / a;
    long long result = horizontal * vertical;
    cout<<result<<endl;
    return 0;
}
